import React, {FC} from 'react';
import logo from "./../../files/logos/logoSmall.png"

const LogoSmall : FC = (props) => {
    return (
        <img src={logo} alt=""/>
    );
}

export default LogoSmall;