import React, {FC} from 'react';
import favourite from './../../files/icons/bookmark.png'

const FavouriteIcon : FC = (props) => {
    return (
        <img src={favourite} />
    );
}

export default FavouriteIcon;