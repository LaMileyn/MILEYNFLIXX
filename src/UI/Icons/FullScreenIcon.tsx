import React, {FC} from 'react';
import fullScreen from './../../files/icons/fullScreen.png'

const FullScreenIcon : FC = (props) => {
    return (
        <img src={fullScreen} />
    );
}

export default FullScreenIcon;